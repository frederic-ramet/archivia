{
  "name": "@archivia/database",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "drizzle-kit generate:sqlite --config=drizzle.config.ts",
    "migrate": "tsx src/migrate.ts",
    "seed": "tsx src/seed.ts",
    "test:opale": "tsx src/test-opale.ts",
    "test:opale:cleanup": "tsx src/test-opale.ts --cleanup",
    "studio": "drizzle-kit studio",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@archivia/shared-types": "workspace:*",
    "@libsql/client": "^0.15.15",
    "drizzle-orm": "^0.44.7"
  },
  "devDependencies": {
    "@types/node": "^20.19.25",
    "drizzle-kit": "^0.20.12",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
