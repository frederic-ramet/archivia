# Configuration Archivia
# Copiez ce fichier en .env et modifiez les valeurs

# Base de données SQLite (libSQL)
DATABASE_URL=file:./packages/database/data/archivia.db

# Environnement (development, production, test)
NODE_ENV=development

# Configuration des uploads
MAX_UPLOAD_SIZE=52428800  # 50MB en bytes
STORAGE_PATH=./public/uploads

# Authentification NextAuth
# Générez une clé secrète avec: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-here-change-this

# Configuration Admin Initial (pour le script seed:admin)
ADMIN_EMAIL=admin@archivia.local
ADMIN_PASSWORD=admin123
ADMIN_NAME=Administrateur

# Services AI
# Note: Ces clés peuvent aussi être configurées via l'interface admin (/admin/settings)
# Les valeurs ci-dessous sont utilisées en fallback si non configurées dans la DB
# OPENAI_API_KEY=sk-your-key-here
# ANTHROPIC_API_KEY=sk-ant-your-key-here

# Configuration de l'OCR
# Peut aussi être configuré via l'interface admin
# OCR_PROVIDER=anthropic  # ou 'openai', 'tesseract'
# OCR_LANGUAGE=fra

# Configuration de production
# NEXT_PUBLIC_BASE_URL=https://your-domain.com
# Pour Turso (production):
# DATABASE_URL=libsql://your-database.turso.io
# DATABASE_AUTH_TOKEN=your-turso-token
